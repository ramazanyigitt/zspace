import 'package:flutter/material.dart';

import 'levels_viewmodel.dart';

class LevelsPage extends StatelessWidget {
  const LevelsPage({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }

  Widget buildLevels() {
    return Container();
  }
}
